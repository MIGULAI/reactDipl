import React from "react";
import { useState } from "react";

const ModalPublSearch = ({ setAuthorId, errCallback }) => {
    const [publs, setPubls] = useState([])

    return (
        <div>

        </div>
    )
}

export default ModalPublSearch